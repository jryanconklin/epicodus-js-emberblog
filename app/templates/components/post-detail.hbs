<div class="spacer">
</div>
<div class="well row">
  <div class="col-sm-8">
    <h2>{{post.title}}<br>
    <small>By {{post.author}}</small></h2>
    <p>{{post.body}}</p>
  </div>
  <div class="col-sm-4">
    <img class="img img-responsive" src={{post.image}} alt="An article by {{post.image}}" />
  </div>
</div>
